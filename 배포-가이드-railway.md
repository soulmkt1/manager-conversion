# Railway 배포 가이드 — DB 대시보드를 웹으로

> 이 앱을 내 PC가 아니라 웹(Railway)에서 24시간 돌리기 위한 안내입니다.
> 코드 준비는 이미 끝났습니다. 아래는 **가입 → 배포 → 데이터 이전** 순서입니다.

## 1단계. 사용자가 직접 하실 일 (계정 준비)
제가 대신 못 하는 부분입니다.
1. **GitHub 계정** 만들기 (github.com) — 무료. 코드를 올려둘 곳.
2. **Railway 계정** 만들기 (railway.com) — GitHub으로 로그인 가능. 결제수단(카드) 등록 필요(월 $5 수준).

두 개 준비되면 알려주세요. 나머지는 제가 진행을 도와드립니다.

## 2단계. 코드 올리기 (제가 도와드림)
- `git init` → GitHub에 비공개(Private) 저장소 생성 → 푸시.
- **환자 데이터(`server/data`)와 비밀번호(`server/config.json`)는 자동 제외**되도록 `.gitignore`에 이미 넣었습니다. (GitHub에 민감정보가 안 올라갑니다.)

## 3단계. Railway 설정 (제가 도와드림)
Railway 프로젝트에서:
- **볼륨(Volume)** 을 `/data` 경로에 추가 → 재배포해도 데이터가 안 사라짐. **⚠️ 이걸 빼먹으면 데이터가 날아갑니다.**
- **환경변수** 등록:
  - `DATA_DIR` = `/data`
  - `APP_PASSWORD` = (원하는 강한 비밀번호)
- 빌드/시작은 자동 감지됩니다(`npm run build` → `npm start`). Node 22는 `package.json`에 명시해뒀습니다.

## 4단계. 기존 데이터 옮기기 (제가 도와드림)
지금 PC의 `server/data/app.db`(약 412KB)를 Railway 볼륨으로 1회 안전하게 올립니다.
- GitHub에 올리지 않고(환자정보 보호), 배포 후 **일회용 보호 업로드**로 옮긴 뒤 즉시 닫습니다.
- 옮긴 뒤 건수(공급/티켓팅/일본상담)를 지금 PC 데이터와 대조해 일치 확인.

## 5단계. 확인
- `https://○○○.up.railway.app` 주소로 접속 → 자물쇠(HTTPS) 확인.
- 새 비밀번호로 로그인 → 전 탭 정상, 건수 일치.
- **데이터 1건 수정 후 재배포 → 그대로 남아있는지**(볼륨 정상 작동) 확인.

## 완료 후
- PC의 `시작하기.bat` / `외부공유.bat`은 더 이상 필요 없습니다(백업용으로 남겨둬도 됨).
- 주소가 고정되고, PC를 꺼도 됩니다.

---
### 참고: 이미 끝난 코드 준비
- DB 저장 위치를 `DATA_DIR` 환경변수로 (영구 볼륨 대응)
- 비밀번호를 `APP_PASSWORD` 환경변수로 (저장소에 평문 안 남김)
- 로그인 무차별 대입 차단(IP별 5분 10회 제한)
- `package.json`에 Node 22 이상 명시
- `.gitignore`에 데이터·비밀번호 파일 제외
